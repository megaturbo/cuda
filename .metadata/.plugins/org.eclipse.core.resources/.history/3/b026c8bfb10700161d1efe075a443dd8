#pragma once

#include "cudaTools.h"

class Slice
{
public:
	Slice(int nbSlice);
	virtual ~Slice(void);

	void run();

private:
	float area(int s);

	// Inputs
	int nbSlice;

	// Tools
	dim3 dg;
	dim3 db;
	size_t sizeOctet;
};
